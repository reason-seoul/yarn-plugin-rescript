{
  "name": "yarn-plugin-rescript",
  "version": "0.0.0",
  "private": true,
  "main": "./sources/index.ts",
  "scripts": {
    "build": "builder build plugin",
    "sync": "yarn build && yarn plugin import ./bundles/@yarnpkg/plugin-rescript.js"
  },
  "dependencies": {
    "@yarnpkg/cli": "^2",
    "@yarnpkg/core": "^2",
    "@yarnpkg/fslib": "^2",
    "@yarnpkg/plugin-pnp": "^2",
    "clipanion": "^2"
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "@yarnpkg/builder": "^2.1.3",
    "@yarnpkg/pnpify": "^3.0.0-rc.7",
    "typescript": "^4.3.5"
  }
}
